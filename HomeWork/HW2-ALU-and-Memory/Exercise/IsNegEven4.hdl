// isEven = LSB == 0
// Negative check = MSB (sign bit) == 1
// If negative → output = isEven, else output = 0

CHIP IsNegEven4 {
    IN a[4];
    OUT out;
    
	PARTS:
	Not(in = a[0], out=isEven);
	Mux(a = false, b = isEven, sel = a[3], out=out);

}
